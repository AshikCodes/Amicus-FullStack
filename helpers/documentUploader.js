const { getStorage, ref, getDownloadURL, uploadBytesResumable } = require('firebase/storage')

const firsebaseStorage = getStorage();
function uploadDocument(file, caseid) {
    

}


module.exports = uploadDocument;